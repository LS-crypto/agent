---
name: code-review
description: 代码审查清单：读 diff、查测试、给可执行建议
---

# 代码审查

## 流程
1. `git diff` 或 `read_file` 了解改动
2. `grep` 查找相关调用与测试
3. 给出分条建议：问题 / 建议 / 可选优化

## 输出格式
- **概要**：改了什么
- **风险**：安全、兼容性、性能
- **测试**：是否需补 pytest
- **结论**：可合并 / 需修改

## 禁止
- 未经确认不要 `write_file` 直接改他人代码
- 不要读取 `.env` 等敏感文件
